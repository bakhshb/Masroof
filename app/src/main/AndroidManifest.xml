<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Read and receive SMS so the inbox reader can display device messages. -->
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <!--
        INTERNET is used ONLY when the user explicitly enables a remote AI
        provider in the AI settings screen. The rest of the application is
        fully offline. The provider short-circuits when disabled, and we
        never open a network connection without an enabled provider config.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Telephony hardware is required for the SMS permissions above. Declared
         as required="false" so the app is installable on devices without
         telephony (e.g. Chrome OS tablets), even though the SMS features will
         not be available there. -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <!--
        Local-only. The app MUST NOT request SEND_SMS. RECEIVE_SMS is
        used only to display messages (no interception / replacement).
        The app MUST NOT modify or delete SMS. android:allowBackup is
        explicitly false so the financial database is never backed up
        to Google's servers or device-to-device transfer.
    -->

    <application
        android:name=".MasroofApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Masroof">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Masroof">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/diagnostic_file_paths" />
        </provider>
    </application>

</manifest>
