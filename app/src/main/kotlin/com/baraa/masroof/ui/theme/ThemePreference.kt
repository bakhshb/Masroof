package com.baraa.masroof.ui.theme

/**
 * User-facing appearance preference.
 *
 * [SYSTEM] follows the OS dark/light setting.
 * [LIGHT] / [DARK] force a fixed color scheme regardless of system.
 */
enum class ThemePreference {
    SYSTEM,
    LIGHT,
    DARK,
}
